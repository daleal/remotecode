services:
  dev:
    build: .
    command: bun run dev
    network_mode: host
    env_file: .env
    environment:
      SLACK_BOT_TOKEN: xoxb-...
      SLACK_APP_TOKEN: xapp-...
    volumes:
      - .:/app
      - /app/node_modules
